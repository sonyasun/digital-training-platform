# 设计还原手册

新开 Chat → 选模型 → 复制 Prompt → 替换 `[...]`。

设计系统：[设计规范.md](./设计规范.md)（令牌真源 `css/tokens.css`）· 落地步骤：[mastergo-frontend SKILL](./.cursor/skills/mastergo-frontend/SKILL.md)

---

## 模型

| 任务 | 模型 |
|------|------|
| MasterGo 整页 / 多文件还原 | Composer 2.5 |
| 对照截图验收 | Gemini 3.x Pro |
| CSS 精修、层次统一 | Claude Sonnet 5 |
| 换 icon、调间距、小改 | Composer 2.5 Fast |

串联：验收清单 → 改代码 → 精修 → 收尾。

---

## Prompt · MasterGo 还原

**Composer 2.5**

```
实现 MasterGo 设计稿到前端。

设计链接：[mastergo.com 链接，含 fileId / pageId / layerId]
目标页面：[新建 xxx.html / 修改 prepare-edit.html / …]
落地范围：[整页 / 弹层 / Tab / 目录树]

要求：
1. 遵循 .cursor/skills/mastergo-frontend/SKILL.md 与 .cursor/rules/design-restore.mdc
2. 先 getDesignSections 拉全 section；文案只用 allTexts / rowTexts
3. 列表行数 = structureSiblingCount
4. 颜色用 _color 并映射 tokens；字号/间距/圆角/阴影用 css/tokens.css
5. 图片本地化到 assets/<语义>/，禁止 MasterGo CDN
6. 复用 topbar / sidebar / rail
7. 完成后列出改动文件与自测步骤

缺 layerId 时先说明，不要猜结构。
```

弹层变体：挂到指定控件；点空白 / Esc 关闭；对齐现有页 CSS。

---

## Prompt · CSS 精修

**Sonnet 5**（小改用 Composer Fast）

```
精修 [文件] 中的 [模块]。

对齐 设计规范.md / tokens.css；只改视觉，最小 diff；保留文案与 data-* 钩子；补全 hover / active / focus-visible。
先简述问题与改法，再改代码。
```

---

## Prompt · 视觉验收

**Gemini 3.x Pro**

```
[附设计截图]

验收 [页面/模块]：布局、间距、颜色、字号、圆角、阴影、icon、hover。
只出不一致清单（高/中/低）+ 建议改的文件，先不写代码。
```

---

## 口令

- `用 MasterGo 还原 prompt，链接是 …`
- `用 CSS 精修 prompt，优化 …`
- `用视觉验收 prompt，对照截图检查 …`
