---
description: 设计还原硬约束（编辑 HTML/CSS 时）
globs: "*.html,css/**/*.css,.cursor/skills/mastergo-frontend/**"
alwaysApply: false
---

# 设计还原

真源：`css/tokens.css` · 用法：[设计规范.md](../../设计规范.md) · Prompt：[设计还原手册.md](../../设计还原手册.md) · 步骤：`mastergo-frontend` skill

## 硬约束

- 文案只来自 MasterGo `allTexts` / `rowTexts`，禁止脑补
- 图片本地化到 `assets/<语义>/`，禁止 MasterGo CDN
- 色 / 字 / 间距 / 圆角 / 阴影 / z-index 用 tokens；字号仅 12–20 阶梯；禁止裸写 8/16/24px 间距
- 表格 / 列表行数 = `structureSiblingCount`
- 复用壳层；教师 `pedit-*` 与学生前缀互不串用；新能力必须有入口
