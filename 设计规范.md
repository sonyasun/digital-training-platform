# 数字实训平台 · 设计规范

**视觉与文案真源**：MasterGo 稿面。  
**代码令牌真源**：[`css/tokens.css`](./css/tokens.css)（色值 / 尺寸以该文件为准，本文只写角色与用法）。  
**字体**：[`css/fonts.css`](./css/fonts.css)

冲突优先级：`MasterGo 稿` > `tokens.css` > 本文用法表 > 单页临时样式 > 课件规范。

---

## 0. 文档地图

| 文档 | 职责 |
|------|------|
| 本文 | 设计系统：令牌用法、壳层、命名、组件契约、交付 |
| [设计还原手册.md](./设计还原手册.md) | Cursor 模型 + Prompt |
| [.cursor/README.md](./.cursor/README.md) | 团队 rules / skills 索引（前端必看） |
| [mastergo-frontend SKILL](./.cursor/skills/mastergo-frontend/SKILL.md) | MasterGo → 代码步骤 |
| [design-restore Rule](./.cursor/rules/design-restore.mdc) | 改 HTML/CSS 硬约束 |
| [COURSEWARE-STYLE.md](./assets/student/courseware/COURSEWARE-STYLE.md) | iframe 课件（镜像本令牌） |

---

## 1. 原则

1. **闭集文案**：可见文字只来自稿面 `allTexts` / `rowTexts` / `node.text`
2. **令牌优先**：色、字号、间距、圆角、阴影、层级用 `var(--*)`，禁止与稿冲突的硬编码
3. **字号阶梯**：仅 12 / 14 / 16 / 18 / 20；禁止 11、13、15、17 等
4. **一端一前缀**：教师 / 学生类名、钩子互不串用
5. **最小 diff**：改视觉不顺手重构；新能力必须有可点入口

---

## 2. 令牌用法

### 2.1 色（角色 → 令牌）

| 角色 | 令牌 | 用于 |
|------|------|------|
| 主文 | `--text-primary` | 标题、正文 |
| 次文 | `--text-secondary` | 说明、导航默认 |
| 弱文 | `--text-muted` | 旁注、分隔 |
| 主色 | `--color-primary` | 主按钮、链接、选中 |
| 成功 / 警告 / 危险 | `--color-success` / `--warning` / `--danger` | 状态 |
| 仿真点缀 | `--color-sim` | 智能/仿真 accent |
| 禁用灰 | `--color-unauth` | 未授权、禁用 |
| 白底 / 页底 / 浅底 | `--bg-white` / `--bg-page` / `--bg-base` · `--bg-grey-3` | 壳与填充 |
| 描边 | `--border-lighter` → `--border-base` | 分割 → 控件边 |

AI 页只用 `--color-ai-*` / `--gradient-ai-btn`，复用现有玻璃态，不新开紫系。

### 2.2 字号（按角色选，不要拍脑袋）

| 角色 | 令牌 | 场景 |
|------|------|------|
| Caption | `--font-12` + `--lh-12` | 角标、页码、TOC |
| Body | `--font-14` + `--lh-14` | **默认**正文、表单、按钮 |
| Emphasize | `--font-16` + `--lh-16` | 卡片题、主行强调 |
| Title | `--font-18` + `--lh-18` | 页/区块标题 |
| Display | `--font-20` + `--lh-20` | 关键数字（一屏少用） |

字重：`--font-weight-regular`（400）、`--font-weight-medium`（500）。  
主字体 Noto Sans SC；`--font-display` 仅 Hero / 品牌标题。

### 2.3 间距（8pt）

| 令牌 | 值 | 用于 |
|------|-----|------|
| `--space-1` | 4 | 图标与字 |
| `--space-2` | 8 | 行内 gap、芯片间距 |
| `--space-3` | 12 | 紧凑内边距、列表 gap |
| `--space-4` | 16 | 区块内边距、工具条 |
| `--space-5` | 24 | 页边距、大块间距 |
| `--space-6` | 32 | 大区块分隔 |
| `--space-7` / `--space-8` | 40 / 48 | 少用 |

新写样式用 `var(--space-*)`；禁止随意 `7px`、`13px`、`18px` 间距。

### 2.4 圆角 / 控件高 / 阴影

| 类型 | 令牌 | 场景 |
|------|------|------|
| 圆角 | `--radius-sm` → `--xl` · `--2xl` · `--pill` | 小控件 → 卡 → 地图卡 → 胶囊 |
| 控件高 | `--control-h-sm` / `md` / `lg` | 28 / 32 / 40 |
| 阴影 | `--shadow-sm` / `md` / `lg` / `drawer` | 轻抬 → 卡片悬停 → 弹层 → 侧抽屉 |
| 焦点 | `--focus-ring` | `focus-visible` |
| 遮罩 | `--overlay-scrim` | 抽屉/弹层 backdrop |

### 2.5 层级

| 令牌 | 场景 |
|------|------|
| `--z-base` | 内容内叠层 |
| `--z-sticky` | 吸顶条 |
| `--z-topbar` | 顶栏 |
| `--z-drawer` | 右侧抽屉（题库/资源/试卷） |
| `--z-dropdown` | 下拉菜单 |
| `--z-modal` | 居中弹窗 |
| `--z-toast` | Toast |

### 2.6 断点

| 令牌 | 数值 | 用途 |
|------|------|------|
| `--bp-sm` | 800px | 窄屏 stack |
| `--bp-md` | 1100px | 双列改单列 |
| `--bp-lg` | 1440px | 宽屏收紧 |

`@media` 写同名像素值（不可 `var()`）。容器查询 / JS 可读令牌。

### 2.7 动效

`transition: … var(--duration) var(--ease)`。尊重 `prefers-reduced-motion: reduce`。

---

## 3. 壳层与端边界

### 3.1 教师端

```
.app
├── .topbar          ← height: var(--top-h); z-index: var(--z-topbar)
└── .shell           ← background: var(--bg-page)
    ├── .sidebar     ← width: var(--sidebar-w)
    ├── .main
    └── .rail        ← width: var(--rail-w)（部分页）
```

页面：`index` · `prepare` · `classes` · `prepare-edit`  
Logo：`assets/brand/logo.png`（182×44）。导航：`js/app.js`。

### 3.2 学生端

侧栏：`css/student.css`（`.student-sidebar`、`.stu-nav-*`）。图标：`assets/icons/student/`。  
**禁止**在学生页复用教师 `pedit-*`、教师壳类名；**禁止**在教师页复用 `stu-*` / `scourse-*` / `saistudy-*` / `nodestudy-*`。

### 3.3 独立全屏

| 页 | 根 |
|----|-----|
| `login.html` | `.login` |
| `student-ai-study.html` | `.saistudy-page` |

自行 `box-sizing`，只引 `fonts` + `tokens` + 页 CSS。

---

## 4. 页面对照

| 页面 | CSS | 前缀 |
|------|-----|------|
| `index.html` | `app.css` | 壳层共用 |
| `prepare.html` | `prepare.css` | `.prep-*` |
| `classes.html` | `classes.css` | `.class-*` |
| `prepare-edit.html` | `prepare-edit.css` | `.pedit-*` |
| `login.html` | `login.css` | `.login-*` |
| `student-courses.html` | `student.css` | `.stu-*` |
| `student-course.html` | `student-course.css` | `.scourse-*` |
| `student-learning-path.html` | `student-learning-path.css` | `.slpath-*` |
| `student-node-study.html` | `student-node-study.css` | `.nodestudy-*` |
| `student-ai-study.html` | `student-ai-study.css` | `.saistudy-*` |

标题：`{页面名} - 广联达数字实训平台V1.0`  
引入：教师页 = `fonts` → `tokens` → `app` → 页 CSS；独立页去掉 `app`。

---

## 5. 命名

BEM：`块__元素--修饰符`，每页唯一前缀。

| 规则 | 做法 |
|------|------|
| 状态 | `is-active` / `is-open` / `is-error`，不用 `--active` |
| JS 钩子 | `data-<prefix>-*` |
| 显隐 | HTML `hidden`（注意 `display:!important` 时补 `[hidden]{display:none!important}`） |
| 教师面包屑 | 只用 `ui-crumb`（`app.css`） |
| 学生面包屑 | 页面前缀变体（玻璃返回钮等） |

---

## 6. 组件契约

新写或还原时对齐；尺寸用令牌。

| 组件 | 契约 |
|------|------|
| 导航项 | 约 176×54；圆角 `--radius-lg`；图标井 32、图标 24 |
| 主按钮 | 高 `--control-h-lg`；底 `--color-primary`；字 `--text-on-primary` |
| 次按钮 / 幽灵 | 高同档；边 `--border-base`；字 `--text-secondary` |
| 输入 | 高 `--control-h-md`～`lg`；边 `--border-base`；圆角 `--radius-md`；focus 用主色边或 `--focus-ring` |
| Chip / Tab | 选中 `is-active`；字 Body |
| 卡片 | 圆角 ≥ `--radius-lg`；hover 可用 `--shadow-md`；必有 `focus-visible` |
| 空状态 | 图标 + 标题 Title/Body + 一句说明 muted；不编造稿外文案 |
| Toast | `z-index: var(--z-toast)`；成功/错误态分明 |
| 居中弹窗 | `z-index: var(--z-modal)`；圆角 `--radius-xl`；遮罩 `--overlay-scrim` |
| 右抽屉 | `z-index: var(--z-drawer)`；宽 `--drawer-w-sm|md|lg`；影 `--shadow-drawer`；Esc + 遮罩关闭；`role="dialog"` `aria-modal` |
| 四态 | default / hover / active / `focus-visible` |
| 禁用 | `opacity` + `cursor: not-allowed`，不删节点 |

教学活动工具栏（`prepare-edit`）：练习 = 题库/添加试题/AI；作业 = 再加课程资料；单元测试/考试 = 选择试卷 + 创建试卷（勿切成练习工具栏）。

---

## 7. 资源

| 目录 | 内容 |
|------|------|
| `assets/brand/` | Logo、侧栏装饰 |
| `assets/icons/` | 教师 `nav-*.png`（含 `-active`）、学生 SVG |
| `assets/course/` · `classes/` · `prepare/` · `student/` · `login/` · `tools/` | 封面与素材 |
| `assets/fonts/` | Noto Sans SC |

禁止 MasterGo CDN；本地化到语义路径；优先 WebP；不留 hash 文件名。导航图标 24×24，操作图标约 20–24。

---

## 8. MasterGo 还原（摘要）

步骤见 skill；Prompt 见手册。

| 规则 | 说明 |
|------|------|
| 文案 | 闭集，禁止脑补 |
| 颜色 | `_color` → 映射到最接近的 tokens |
| 行数 | = `structureSiblingCount` |
| 壳 | 复用 topbar / sidebar / rail |
| 图 | 本地化 |
| 入口 | 新能力必须可点到达 |

---

## 9. 无障碍

- `lang="zh-CN"`
- 图标按钮：`aria-label`
- Tab：`tablist` / `tab` / `aria-selected`
- 弹层：`dialog` + `aria-modal` + Esc
- 装饰图：`alt=""` + `aria-hidden="true"`

---

## 10. Vue 例外

学习地图：`components/**` → `npm run build:learning-map` → `js/learning-map.bundle.js`。改源必重编。

---

## 11. 交付清单

- [ ] 色 / 字 / 间距 / 圆角 / 阴影 / z 来自 tokens（或稿面 `_color` 已映射）
- [ ] 新样式禁止裸写 `8px`/`16px`/`24px` 间距与 `0 8px 24px` 阴影，改用 `--space-*` / `--shadow-*`
- [ ] 无 MasterGo 外链图；前缀与端别正确
- [ ] 四态与 `is-active` 正确；弹层可 Esc / 遮罩关
- [ ] 新功能有入口
- [ ] 改 Vue 已 build；部署前 `npm run predeploy`
