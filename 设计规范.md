# 数字实训平台 · 设计规范

> 本文档汇总项目现有设计令牌、布局壳层、命名约定与还原规则，作为设计与开发的统一参考。  
> MasterGo 稿面仍是视觉与文案的**唯一来源**；本规范描述「如何在代码里落地」。

---

## 1. 项目概览

| 维度 | 说明 |
|------|------|
| 形态 | 静态 HTML / CSS / JS，少量 Vue 组件经 Vite 打包为 IIFE |
| 设计源 | MasterGo（课程中心、备课中心、班课、学生端、登录等） |
| 令牌入口 | `css/tokens.css`、`css/fonts.css` |
| 还原流程 | `.cursor/skills/mastergo-frontend/SKILL.md`、[设计还原手册.md](./设计还原手册.md) |

---

## 2. 设计令牌

所有页面应优先使用 `css/tokens.css` 中的 CSS 变量，避免硬编码与设计稿冲突的颜色。

### 2.1 文字色

| 令牌 | 值 | 用途 |
|------|-----|------|
| `--text-primary` | `#303133` | 标题、正文主色 |
| `--text-secondary` | `#606266` | 次要文字、导航默认态 |
| `--text-muted` | `#909399` | 辅助说明、分隔符 |
| `--text-dark` | `#3d3d3d` | 备课 Hero 等深色标题 |
| `--text-ink` | `#000000` | 极少数强调 |
| `--text-on-primary` | `#ffffff` | 主色按钮上的文字 |

### 2.2 品牌与功能色

| 令牌 | 值 | 用途 |
|------|-----|------|
| `--color-primary` | `#1677ff` | 主操作、链接 hover、选中强调 |
| `--color-success` | `#28b28b` | 发布、完成、正向状态 |
| `--color-warning` | `#dca707` | 警告 |
| `--color-sim` | `#575aff` | 仿真/智能相关 accent |
| `--color-unauth` | `#bfbfbf` | 未授权、禁用 |

### 2.3 背景色

| 令牌 | 值 | 用途 |
|------|-----|------|
| `--bg-white` | `#ffffff` | 卡片、顶栏、侧栏 |
| `--bg-page` | `#ebedf0` | 主内容区铺底（`.shell`） |
| `--bg-base` | `#f0f2f5` | 独立页全屏背景 |
| `--bg-grey-3` | `#f5f5f5` | 浅灰填充 |
| `--bg-search` | 渐变 | 搜索条背景 |

### 2.4 边框

| 令牌 | 值 |
|------|-----|
| `--border-lighter` | `#ebeef5` |
| `--border-light` | `#e4e7ed` |
| `--border-base` | `#dcdfe6` |
| `--border-grey-5` | `#d9d9d9` |

### 2.5 字号与行高

| 令牌 | 值 | 配对行高 |
|------|-----|----------|
| `--font-12` | 12px | `--lh-12` 20px |
| `--font-14` | 14px | `--lh-14` 22px（**正文默认**） |
| `--font-16` | 16px | `--lh-16` 24px |
| `--font-18` | 18px | `--lh-18` 26px |
| `--font-20` | 20px | `--lh-20` 28px |

字重：`--font-weight-regular` (400)、`--font-weight-medium` (500)。  
**默认正文字号** `--font-body` / `--font-14`（14px）；**最小字号** `--font-min`（12px），全站不得小于 12px。

### 2.6 圆角

| 令牌 | 值 | 典型场景 |
|------|-----|----------|
| `--radius-sm` | 6px | 小控件 |
| `--radius-md` | 8px | 按钮、输入框 |
| `--radius-lg` | 12px | 导航项、卡片 |
| `--radius-xl` | 16px | 大卡片、弹层 |
| `--radius-pill` | 100px | 胶囊按钮、标签 |

### 2.7 布局尺寸

| 令牌 | 值 | 说明 |
|------|-----|------|
| `--sidebar-w` | 200px | 教师端左栏 |
| `--rail-w` | 100px | 教师端右轨 |
| `--top-h` | 60px | 顶栏高度 |

### 2.8 动效

| 令牌 | 值 |
|------|-----|
| `--ease` | `cubic-bezier(0.22, 1, 0.36, 1)` |
| `--duration` | `180ms` |

交互过渡统一：`transition: … var(--duration) var(--ease)`。

---

## 3. 字体

- **主字体**：Noto Sans SC（等同思源黑体），本地 woff2，见 `css/fonts.css`
- **展示字体**：`--font-display`（DingTalk JinBuTi），仅 Hero/品牌标题等特殊场景
- **页面引入顺序**：`fonts.css` → `tokens.css` → 页面 CSS
- **抗锯齿**：`body` 使用 `-webkit-font-smoothing: antialiased`

---

## 4. 布局壳层

### 4.1 教师端 · 三栏壳（带顶栏）

适用于：`index.html`、`prepare.html`、`classes.html`、`prepare-edit.html`（部分）

```
.app
├── .topbar          ← 60px，白底，底边框
└── .shell           ← flex 行，背景 --bg-page
    ├── .sidebar     ← 200px，主导航
    ├── .main        ← flex:1，页面内容
    └── .rail        ← 100px，快捷工具轨（部分页面）
```

- 顶栏品牌：Logo 182×44、`assets/brand/logo.png`
- 侧栏装饰：`assets/brand/sidebar-deco.webp`（底部，旋转 180°）
- 导航选中：`.nav-item.is-active`、子组 `.nav-group.is-open`
- 逻辑：`js/app.js`（`data-nav-group` / `data-nav-toggle`）

### 4.2 学生端 · 侧栏 + 主区

适用于：`student-courses.html`、`student-course.html`

- 侧栏样式：`css/student.css`（`.student-sidebar`、`.stu-nav-*`）
- 图标目录：`assets/icons/student/`
- 主区 modifier：`.main--student-course` 等

### 4.3 独立全屏页（无顶栏/侧栏）

适用于：`login.html`、`student-ai-study.html`

| 页面 | 根 class | 布局 |
|------|----------|------|
| 登录 | `.login` | 左右分栏卡片 |
| AI 学习 | `.saistudy-page` > `.saistudy` | 面包屑 + 双列 grid |

独立页在 `body` 或根容器内**自行 reset** `box-sizing`，并复用 tokens 变量。

---

## 5. 页面与 CSS 命名约定

采用 **BEM 变体**：`块__元素--修饰符`，**每页有唯一前缀**，避免全局污染。

| 页面 | HTML | CSS | JS 前缀 | 说明 |
|------|------|-----|---------|------|
| 课程中心 | `index.html` | `app.css` | — | 共用壳层类名 |
| 备课中心 | `prepare.html` | `prepare.css` | `.prep-*` | Hero + 卡片网格 |
| 我的班课 | `classes.html` | `classes.css` | `.class-*` | 班课卡片 |
| 课程编辑 | `prepare-edit.html` | `prepare-edit.css` | `.pedit-*` | 目录树、添加弹层 |
| 登录 | `login.html` | `login.css` | `.login-*` | `data-login-*` |
| 学生课程列表 | `student-courses.html` | `student.css` | `.stu-*` | `data-stu-*` |
| 学生课程详情 | `student-course.html` | `student-course.css` | `.scourse-*` | `data-scourse-*` |
| AI 个性化学习 | `student-ai-study.html` | `student-ai-study.css` | `.saistudy-*` | `data-saistudy-*` |

### 5.1 命名规则

1. **块**：页面语义缩写 + 连字符（`pedit-add`、`saistudy-crumb`）
2. **元素**：双下划线 `__`（`saistudy-crumb__link`）
3. **状态**：`is-*` 类（`is-active`、`is-open`），不用 BEM `--active`
4. **JS 钩子**：`data-<prefix>-*`（`data-pedit-add`、`data-saistudy-agent-threshold`）
5. **布局容器**：双下划线语义（`saistudy__layout`、`shell`）

### 5.2 面包屑模式

各页结构一致，仅前缀不同：

```
.{prefix}-crumb
├── .{prefix}-crumb__back      ← 返回箭头 24–28px 点击区
├── .{prefix}-crumb__link      ← 可点击层级
├── .{prefix}-crumb__sep       ← `/`
└── .{prefix}-crumb__current   ← 当前页，medium 字重
```

参考：`scourse-crumb`、`saistudy-crumb`、`pedit-crumb`。

---

## 6. 组件与模式

### 6.1 导航项（教师端）

- 尺寸：176×54px，圆角 `--radius-lg`
- 图标容器：32×32 圆形容器，图标 24×24
- 选中：文字 `--text-primary`，图标容器高亮背景
- 子菜单：`nav-sub`，父级 `nav-group.is-open`

### 6.2 课程卡片

- 文件：`app.css` → `.course-card`
- Hover：`box-shadow` + 轻微 `transform`
- 必须：`focus-visible` 轮廓（键盘可达）

### 6.3 筛选 Chip

- `.filter-chip` / `.filter-chip.is-active`
- 激活：主色边框或填充

### 6.4 Tab

- 登录：`.login-tabs__item.is-active`
- 学生：`.stu-tabs__item.is-active` + 底边指示
- AI 演示：`.demo-tab.active`（Vue 组件内）

### 6.5 弹层 / Popover

- 备课添加：`.pedit-add`（圆角 16px，阴影 `0 12px 40px rgba(48,49,51,.16)`）
- 关闭：Esc、点遮罩（`prepare-edit` 模式）
- `hidden` 属性控制显隐，不用仅 CSS `display:none` 做唯一开关

### 6.6 表单

- 输入框：高度约 32–40px，边框 `--border-base`，圆角 `--radius-md`
- Focus：边框/阴影主色淡化，参考 `.login-field--focus`
- 主按钮：高度 40px 左右，主色底或渐变

---

## 7. AI 视觉语言

AI 相关模块在 tokens 之外有一套**紫色系**扩展，用于个性化学习、知识图谱、助手气泡。

| 语义 | 典型色值 | 出现位置 |
|------|----------|----------|
| `--color-ai-accent` | `#8800ff` | AI 渐变终点、强调 |
| `--color-ai-bg-soft` | `#f5f0ff` | 通知条、标签浅底 |
| `--color-ai-bg-page-top` / `--color-ai-bg-page-mid` | `#f3f0ff` / `#eef5ff` | AI 页背景渐变 |
| `--gradient-ai-btn` | primary → ai-accent | AI 主按钮渐变 |
| `--focus-ring` | `0 0 0 3px rgba(22,119,255,.25)` | 键盘 focus 轮廓 |
| `--radius-2xl` | `20px` | 大地图卡片等 |
| 路径绿 | `#10b981` / `#34d399` | 学习路径动画 |
| 补强橙 | `#f59e0b` | 动态补强分支 |
| 拓展蓝 | `#2563eb` / `#3b82f6` | 拓展节点 |

**原则**：新 AI 页面优先沿用 `student-ai-study.css`、`AdaptivePathSignatureModal` 已有渐变与玻璃态（`backdrop-filter` + 半透明白），不要另起一套色相。

---

## 8. 交互状态

所有可点击元素需完整四态（能用则用）：

| 状态 | 实现要点 |
|------|----------|
| Default | tokens 颜色 |
| Hover | 背景淡化 / 阴影加深 / 文字变 `--color-primary` |
| Active | `transform: translateY(1px)` 或阴影减弱 |
| Focus-visible | `outline: 3px solid rgba(22, 119, 255, 0.25)` 或项目内等价物 |

- 过渡时长：`var(--duration)`，缓动：`var(--ease)`
- 禁用：`opacity` + `cursor: not-allowed`，不用移除元素
- 动画偏好：Vue 组件内尊重 `prefers-reduced-motion: reduce`

---

## 9. 图标与图片

### 9.1 图标

| 类型 | 路径 | 说明 |
|------|------|------|
| 教师导航 | `assets/icons/nav-*.png` | 含 `-active` 双态 |
| 用户操作 | `assets/icons/user-*.svg` | 编辑、复制 |
| 学生导航 | `assets/icons/student/*.svg` | SVG 为主 |
| 备课活动/素材 | `assets/prepare/activity/`、`material/`、`resource/` | 按类型分目录 |

- 导航图标：**24×24** 显示尺寸
- 操作图标（加号、编辑）：**20–24px**，方向与稿一致（多为横向三点，非竖向小点）
- 内联 SVG：放在 `.app svg` 或对应容器内，`display:block; width/height:100%`

### 9.2 图片资源

| 目录 | 内容 |
|------|------|
| `assets/brand/` | Logo、侧栏装饰、旗舰版角标 |
| `assets/course/` | 课程封面 `cover-*.webp` |
| `assets/classes/` | 班课封面、头像 |
| `assets/prepare/` | 备课 Hero、工具图标、图谱 IP |
| `assets/student/` | 学生端 AI 路径、纹理 |
| `assets/login/` | 登录页背景与装饰 |
| `assets/tools/` | 软件图标、助手形象 |
| `assets/fonts/` | Noto Sans SC woff2 |

**硬性规则**：

1. **禁止** MasterGo CDN（`image-resource.mastergo.com`）出现在 HTML/CSS/JS
2. 下载后按语义目录重命名，优先 **WebP**（`npm run optimize-images`）
3. 代码只引用语义路径，不保留 hash 文件名

---

## 10. MasterGo 还原规则（摘要）

完整流程见 [mastergo-frontend SKILL](./.cursor/skills/mastergo-frontend/SKILL.md)。

| 规则 | 说明 |
|------|------|
| 文案闭集 | 仅 `allTexts` / `rowTexts` / `node.text`，禁止脑补 |
| 颜色 | 优先 DSL `_color`，并对齐 tokens |
| 行数 | 表格/列表行数 = `structureSiblingCount` |
| 布局 | section 绝对定位 bbox；flex 容器用 `flexContainerInfo` |
| 壳层 | 复用现有 topbar/sidebar/rail，不整页重写导航 |
| 图片 | 本地化到 `assets/<语义>/` |
| 入口 | 新页必须挂链接/按钮，否则视为未完成 |

---

## 11. 响应式

| 断点 | 用途 |
|------|------|
| ≤880px | 签名弹层/地图 demo 单列（组件内） |
| ≤768px | 学生 AI 页双列 Tab、布局 stack |
| ≤620px | 弹层缩放、字号缩小 |

常用手法：`clamp()` 做 padding/字号、grid 改 `1fr`、`minmax()` 防溢出。

---

## 12. 无障碍

- 语言：`<html lang="zh-CN">`
- 图标按钮：`aria-label`
- Tab：`role="tablist"` / `role="tab"` / `aria-selected`
- 弹层：`role="dialog"`、`aria-modal`、Esc 关闭
- 装饰图：`alt=""` + `aria-hidden="true"`（纯装饰时）

---

## 13. 技术例外 · Vue 组件

仅 **AI 学习路径演示** 使用 Vue 3 + GSAP：

| 路径 | 说明 |
|------|------|
| `components/business/AdaptivePathSignatureModal.vue` | 四场景 SVG 动画 |
| `components/LearningMapPresentationMount.vue` | 嵌入 mount + 深度样式 |
| `js/learning-map-entry.js` | 挂载 `#learning-map-root` |
| `js/learning-map.bundle.js` | Vite IIFE 产物 |
| 构建 | `npm run build:learning-map` |

修改 Vue 源文件后**必须重新 build**，静态页引用的是 bundle 而非源码。

---

## 14. 文件与样式引入

### 14.1 新教师页模板

```html
<link rel="stylesheet" href="css/fonts.css" />
<link rel="stylesheet" href="css/tokens.css" />
<link rel="stylesheet" href="css/app.css" />
<link rel="stylesheet" href="css/<page>.css" />
```

### 14.2 新独立页模板

```html
<link rel="stylesheet" href="css/fonts.css" />
<link rel="stylesheet" href="css/tokens.css" />
<link rel="stylesheet" href="css/<page>.css" />
```

### 14.3 页面标题

格式：`{页面名} - 广联达数字实训平台V1.0`

---

## 15. 自检清单

交付前对照：

- [ ] 颜色/字号/圆角来自 `tokens.css` 或与 MasterGo `_color` 一致
- [ ] 未引入 MasterGo 外链图片
- [ ] 类名前缀与页面一致，无跨页复用冲突
- [ ] hover / active / focus-visible 完整
- [ ] 导航/current 态正确（`is-active` / `is-open`）
- [ ] 新功能有入口链接
- [ ] 改 Vue 组件已执行 `npm run build:learning-map`
- [ ] 部署前 `npm run predeploy`（含图片优化与 dist 同步）

---

## 16. 相关文档

| 文档 | 用途 |
|------|------|
| [设计规范.md](./设计规范.md) | 本文 · 设计系统总览 |
| [设计还原手册.md](./设计还原手册.md) | Cursor Prompt / 模型选型 |
| [.cursor/skills/mastergo-frontend/SKILL.md](./.cursor/skills/mastergo-frontend/SKILL.md) | MasterGo 落地流程 |
| [.cursor/rules/design-restore.mdc](./.cursor/rules/design-restore.mdc) | 编辑 HTML/CSS 时自动规则 |
| [css/tokens.css](./css/tokens.css) | 设计令牌源文件 |
| [README.md](./README.md) | 本地预览与部署 |

---

*最后整理：2026-08 · 随 MasterGo 稿面与代码演进同步更新 tokens 与页面表。*
